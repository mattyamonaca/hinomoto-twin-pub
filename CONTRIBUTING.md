# 開発・検証への参加

公開統計に基づくペルソナ分布を、出典と仮定を追跡できる形で改善するためのプロジェクトです。現在の推定対象と機能は [METHOD](METHOD.md)、検証状況は [検証結果と限界](docs/VALIDATION.md)を参照してください。

## 変更時に残す情報

- 新しい統計を使う場合：正式な表名、提供元URL、表ID、調査年・地域境界、対象集団、単位、所得の定義、取得日、原ファイルのハッシュ、加工方法。
- 推定を変更する場合：観測値と仮定の区別、周辺制約、欠測・0・秘匿・丸めの扱い、変更前後の検証値、検証データが学習に使われていない範囲。
- 地域を変更する場合：市と区の二重計上、合併・行政区再編、人口0、コード先頭の0を確認。

## 作業手順

Python 3.11以上の仮想環境で `make install`、`make dataset`、`make build`、`make verify` を実行します。元Excelから加工する場合は `make install-source` の後、元ファイルがあれば `make prepare`、なければ `make fetch` を使います。makeを使わない場合はREADMEのPython実行例に従ってください。

コード側では `src/`・`site/`・`catalog/` と文書を管理します。元データ、正規化済み入力、計算結果、検証結果、画面用データはすべて外部データルートに保存します。配置とデータの受け渡しは [DATA.md](DATA.md) を参照してください。大量の出力を配布するときはバージョン付きの別アーカイブを作り、再計算可能な入力・パラメータと対応づけてください。

パイプラインの統計的変更は `METHOD.md`・`docs/VALIDATION.md` と対応する検証結果を同じ変更で更新してください。単に既知の周辺合計に一致したことだけで、細かい同時分布の精度が実証されたとは判断しません。公表の復元人口は実標本人数ではありません。

## 優先したい改善

1. 人口・就業・所得・地域境界の時点統一。
2. 公開済み属性の仮定の検証と、職業・勤務時間など未対応属性の検討。
3. 小規模町村・行政区での外部検証。
4. 所得不詳、就業定義の差、平滑化係数に対する感度分析。
5. データ取得画面の変更検出と、表構造・単位・符号に関する検証の充実。

## ライセンス

独自のコード・文書は [Apache License 2.0](LICENSE) で公開します。意図的に提出する貢献にも同ライセンスを適用します。第三者のコード・素材を追加する場合は、そのライセンスと必要な表示を保持してください。統計データの条件は [DATA_LICENSE](DATA_LICENSE.md)を参照してください。

## データを使わないテスト

`make install-source` の後 `make test` を実行します。全国データを使わず、設定の切替、インポート時の副作用、データ形式、上書き拒否、アーカイブの整合性を確認します。統計計算を変える場合は、外部データを使う `make build`・`make verify` も必要です。

文書はREADMEを入口とし、現在の方法はMETHOD.md、現在の検証状況はdocs/VALIDATION.md、再現手順はDATA.mdへ更新します。新しいモデル識別子ごとに利用者向け説明書を増やさず、過去の文書はGitの履歴、検証根拠は詳細実験記録で参照できるようにしてください。

## 画面の検証

Node.js 20以上を使用し、リポジトリのルートで `npm ci` を実行します。検証用のjsdomは `package-lock.json` で固定しています。ブラウザ利用者にはNode.jsは不要です。

画面とPythonの一致を確かめる場合は、同じデータルートで `make export-employment-web` と `make export-workplace-web` を実行して比較用レポートを生成し、`make verify-web-employment`・`make verify-web-workplace`・`make verify-web-derivations` を実行してください。基本の `make test` だけでは全国データを使う画面検証は実行されません。
