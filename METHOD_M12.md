# 本番モデル v3.0-M12

2026-09-06に公開モデルをM0からM1+M2へ切り替え。係数は全都市で選択した学歴1.5、産業1.0、税務0。年齢・性別・学歴・所得の区分、15歳以上、2020年境界・人口と2022年年間就業収入の定義は従来と同じ。

地域の就業構成による混合分布に、全国の学歴別所得形状から計算した地域/県の学歴構成の傾きの1.5乗と、全国の産業別所得形状から計算した産業構成の傾きを掛ける。その後、地域の有給就業人数と県×性別×年齢の所得既知有業者の構成比へ調整する。非就業等の0円を加え、地域の学歴人数に合わせて配分する。出力では0円と50万円未満を統合する。

## 検証・公開判断

86都市1,118セルの開発データにおける交差検証TVはM0 0.09327747、M12 0.08987450（相対3.65%改善）。これは全都市の固定係数で生成する公開値の学習内指標とは異なる。M1比の追加改善は約0.14%。都市表は県集計にも含まれ、開発で繰り返し使用している。独立テスト地域の評価は未実施であり、町村・行政区・性別別・学歴別・非就業者を含む分布の実測精度は未検証。M2/M12の仮想人口による復元評価も未実施。

利用者の本番反映依頼に基づく公開切り替えであり、これらの未検証範囲が解消されたという判断ではない。旧M0は再生成・旧リリースへのロールバックが可能。

2026-09-07 の追補（Issue #21、[docs/VALIDATION_M12.md](docs/VALIDATION_M12.md)）：M2/M12 を含む仮想人口の復元評価を実施し、学歴×産業の交互作用・産業効果の年齢依存や地域差・情報のある不詳・小規模地域・産業無関係の各条件で M12 が M0 より悪化しないことを確認した（小規模地域と産業無関係では差なし）。独立評価として、どのモデルの入力でもない令和5年度 市町村税課税状況等の調（2022年所得、全1,740市区町村）との県内相対水準の整合を事前登録した規則で確認し、M12 は支持された（市・政令市・特別区で相関 0.86〜0.92）。ただし町村では M12 の整合は税務指標を使わない M0 と同水準（0.48）で M1（0.60）より低く、産業構成の傾きは町村で独立指標と逆向きに動く傾向がある。町村・行政区の所得分布そのものの実測比較は引き続き不可能（公表統計がない）。都市表の当てはまりは γ ∈ [1.0, 2.0]、γ₂ ∈ [0.5, 1.5] でほぼ平坦で、係数の値は強く特定されない。

学歴・産業の傾きの乗算は個人属性の条件付き独立を保証せず、相関情報を重複して使う可能性がある。産業別所得形状に年齢がなく、全国の形状を移植する仮定が残る。在学中は独立区分、学歴不詳も他区分に再配分しない。

## 再現

データとコードを別管理する。`make dataset`は固定リリースの正規化統計と公開用graphを外部データルートへ取得する。`make build`はM12を生成し、3属性・5属性出力と公開graphを更新する。`python src/pipeline.py build --model M0`で旧モデルの数値を再生成できる。旧版サイトへの切り戻しには旧コミットと対応する旧データの組を使用する。

```sh
export HINOMOTO_DATA_ROOT=../hinomoto-twin-data-m12
make install
make dataset
make build
make verify
make site
```

`src/build_production.py`がM12を選択し、`src/export_web.py`が最終配列から画面用配列を生成する。公開graphのschema_version=2はlittle-endian float64配列をbase64化し、旧版の16ビット量子化を使わない。年齢・性別・学歴人口と所得配列の一致、県・全国の集計、CSV/JSONの値を検証する。公開用の地理・原表メタデータは固定スナップショットを引き継ぐ。

新モデルの`model_metadata.json`を出力し、M0構築時の中間レポート`tax_summary.json`等は公開M12の精度と混同しない。M0の学歴感度分析の数値はM12の画面に流用しない。比較の詳細は[実験記録](docs/EXPERIMENT_M12.md)、元の定義は[METHOD.md](METHOD.md)を参照。
